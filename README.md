# CodeBlocks-Overloaded-Functions
Overloaded Functions
Two overloaded functions. Overloaded functions have the same 
name, but either different numbers of parameters, or the same number of parameters, but of 
different types
